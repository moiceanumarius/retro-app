# Production Environment Configuration
# Copy this file to .env.prod and configure with your production values

# Application
APP_ENV=prod
APP_DEBUG=false
APP_SECRET=your-super-secret-key-here-change-this-in-production

# Database
MYSQL_ROOT_PASSWORD=your-super-secure-root-password
MYSQL_USER=retro_user
MYSQL_PASSWORD=your-super-secure-user-password

# Redis
REDIS_PASSWORD=your-redis-password

# Mercure (Real-time updates)
MERCURE_URL=https://your-domain.com/.well-known/mercure
MERCURE_PUBLIC_URL=https://your-domain.com/.well-known/mercure
MERCURE_JWT_SECRET=your-mercure-jwt-secret-key-change-this

# Mailer (configure with your SMTP provider)
MAILER_DSN=smtp://user:pass@smtp.example.com:587

# Security
# Generate a new secret key for production:
# php -r "echo bin2hex(random_bytes(32));"

# SSL Configuration
# Place your SSL certificates in docker/nginx/ssl/
# - cert.pem (certificate file)
# - key.pem (private key file)

# Domain Configuration
# Update server_name in docker/nginx/nginx-prod.conf to match your domain
